# Root Recovery Helper — 자동 필수 모듈 재활성화 PRD

## 1. 목적

이 문서는 `feature/auto-reenable-required-modules` 브랜치의 동작을 정의한다.

기존 방식은 LSPosed 또는 관련 Magisk 모듈의 비활성화 상태를 감지한 뒤 사용자가 Magisk에서 직접 모듈을 다시 활성화하도록 안내한다. 이 브랜치에서는 사전에 지정한 필수 모듈에 한해 앱이 `disable` 파일을 자동으로 삭제하고, 이어서 Magisk의 Zygisk 설정도 활성화한다.

## 2. 범위

앱은 부팅 완료 후 약 10초 뒤 `/data/adb/modules/zygisk_lsposed/disable` 파일과 Magisk의 Zygisk 설정을 함께 검사하여 루팅 해제 여부를 판정한다. `disable` 파일이 있거나 Zygisk가 꺼져 있을 때 사용자에게 루팅 복구 여부를 확인한다. 사용자가 `루팅 복구`를 선택한 경우에는 아래 허용 목록 6개 전체를 검사한다.

```text
/data/adb/modules/magisk-einklauncher/
/data/adb/modules/magisk-insecure-adb/
/data/adb/modules/magisk-rp4posed/
/data/adb/modules/refreshpaper/
/data/adb/modules/rp400-epdd-patch/
/data/adb/modules/zygisk_lsposed/
```

위 경로들은 모두 `/data/adb/modules`의 직접 하위 디렉터리다. 목록 밖의 모듈은 자동 복구 대상이 아니다.

## 3. 핵심 동작

### 3.1 부팅 후 검사

1. `BOOT_COMPLETED`를 수신한다.
2. 부팅 직후에는 상태를 변경하지 않고 약 10초 뒤 작업을 예약한다.
3. 하나의 root 셸 세션에서 `/data/adb/modules/zygisk_lsposed/disable`을 읽기 전용으로 검사하고 Magisk 설정 DB의 `zygisk` 값을 조회한다.
4. `disable` 파일이나 비활성화된 Zygisk가 감지되었거나 두 상태 중 하나라도 명확히 확인하지 못한 경우에만 같은 세션에서 ADB를 강제로 활성화한다. 두 상태가 모두 정상이면 ADB 설정을 변경하지 않는다.
5. `disable` 파일이 있거나 `zygisk` 값이 `1`이 아니면 후속 설정 대기 상태보다 우선하여 `루팅 해제가 감지되었습니다!` Dialog를 표시한다.
6. `disable` 파일이 없고 Zygisk가 활성화되어 있으며 LSPosed 후속 설정 대기 상태이면 LSPosed 설정 Dialog를 표시한다.
7. 두 상태가 모두 정상이고 후속 설정 대기 상태도 아니면 아무 Dialog도 표시하지 않는다.
8. 둘 중 하나라도 비활성화된 것이 명확하면 다른 상태를 확인할 수 없어도 복구 Dialog를 표시한다. 명시적인 비활성화 없이 검사 결과만 불명확하면 어떤 Dialog도 표시하지 않고 오류를 기록한다.
9. 사용자가 `루팅 복구`를 누르면 자동 삭제 화면으로 이동한다.
10. 사용자가 `무시`를 누르면 모듈 파일과 Zygisk 설정을 변경하지 않고 종료한다. 복구 Dialog가 표시되기 전 조건부 ADB 활성화는 이미 시도된 상태다.
11. 자동 삭제 화면에서는 하나의 root 셸 세션으로 각 허용 목록 디렉터리의 `disable` 파일 존재 여부를 확인한다.

### 3.2 자동 재활성화

자동 재활성화는 사용자가 복구 Dialog에서 `루팅 복구`를 선택한 경우에만 시작한다.

허용 목록의 디렉터리 아래에 다음 파일이 있으면 해당 파일만 삭제한다.

```text
<허용 목록 모듈 디렉터리>/disable
```

처리 규칙:

- 모듈 디렉터리가 없으면 건너뛴다.
- `disable` 파일이 없으면 건너뛴다.
- `disable` 파일이 있으면 파일만 삭제한다.
- 모듈 디렉터리 자체, `module.prop`, 스크립트, APK 및 다른 파일은 삭제하거나 수정하지 않는다.
- 허용 목록에 없는 모듈에는 명령을 실행하지 않는다.
- 파일 삭제는 모듈의 현재 실행 상태를 즉시 변경하지 않으며, 모듈 재활성화는 다음 재부팅부터 적용될 수 있다.

허용 목록 6개를 모두 처리한 직후 같은 root 셸 세션에서 다음 순서로 Zygisk를 처리한다.

1. 설치된 Magisk가 `magisk --sqlite` 명령을 제공하는지 실제 명령 결과로 확인한다.
2. Magisk 설정 DB의 `zygisk` 값을 조회한다.
3. 이미 `1`이면 변경하지 않고 활성화 상태로 기록한다.
4. `0`이거나 설정 행이 없으면 `zygisk=1`을 기록한다.
5. 값을 다시 조회하여 `1`인지 검증한다.

특정 Magisk 버전 번호를 전제로 분기하지 않는다. 명령 미지원, DB 접근 실패, 기록 실패 또는 재조회 불일치는 모두 Zygisk 활성화 실패로 처리한다.

### 3.3 처리 진행 표시

자동 삭제 작업 중에는 화면에 현재 처리 중인 모듈을 표시한다.

예시:

```text
재활성화 준비 중
처리 중: zygisk_lsposed
disable 파일 삭제 완료: zygisk_lsposed
처리 중: refreshpaper
disable 파일 없음: refreshpaper
```

각 허용 목록 모듈은 순서대로 한 번씩 처리한다. 처리 결과는 `삭제 완료`, `파일 없음`, `대상 없음`, `실패`, `확인 불가` 중 하나로 표시한다.

모듈 처리가 끝나면 `Zygisk 활성화 중`, `활성화 완료`, `이미 활성화됨`, `활성화 실패` 또는 `명령 미지원` 상태를 실시간으로 이어서 표시한다.

### 3.4 재부팅

다음 조건을 모두 만족하면 자동 재부팅을 예약한다.

- 하나 이상의 `disable` 파일을 삭제했거나 Zygisk를 새로 활성화함
- 허용 목록의 모든 대상에 대한 처리가 완료됨
- 삭제 또는 상태 확인 실패가 없음
- Zygisk 설정이 활성화된 것으로 재조회 검증됨

모든 처리가 완료되면 LSPosed 후속 설정 대기 상태를 저장하고 화면에 다음 문구와 카운트다운을 표시한다.

```text
루팅 복구 준비 완료
10초 후 재부팅합니다.
```

카운트다운이 끝나면 root 권한으로 기기를 재부팅한다. 다음 부팅 약 10초 후에는 LSPosed 설정 Dialog를 표시한다. 삭제 대상이 하나도 없고 Zygisk도 이미 활성화되어 있거나, 모듈 처리 또는 Zygisk 활성화 중 오류가 발생한 경우에는 후속 설정 상태를 저장하지 않고 자동 재부팅하지 않는다.

### 3.5 ADB 조건부 활성화

부팅 완료 후 약 10초가 지나면 먼저 두 복구 조건을 검사한다. `zygisk_lsposed/disable` 파일이 존재하거나 Zygisk가 비활성화된 경우에는 `RECOVERY_DETECTED`, 파일 또는 Zygisk 상태를 명확히 확인하지 못한 경우에는 `STATE_CHECK_FAILED`로 기록하고 ADB 활성화를 시도한다. 파일이 없고 Zygisk가 활성화된 정상 상태는 `NORMAL`로 기록하고 ADB 설정을 변경하지 않는다. 상태 검사와 조건부 ADB 처리는 하나의 root 셸 세션으로 실행한다.

사용자가 복구 Dialog에서 `루팅 복구`를 선택해 자동 재활성화 화면으로 이동한 경우에는 이미 복구 필요 상태가 확인된 것이므로 같은 명령을 다시 실행하여 복구 중 ADB 상태를 유지한다.

```sh
resetprop ro.adb.secure 0
settings put global adb_enabled 1
setprop ctl.start adbd
```

세 명령의 실행 여부와 결과, ADB 활성화 판정 사유, 각 모듈의 삭제 결과를 별도로 기록한다. `magisk-insecure-adb` 모듈이 비활성화된 부팅에서도 PC가 ADB 연결을 사용할 수 있도록 `resetprop ro.adb.secure 0`을 ADB 설정 및 데몬 시작보다 먼저 실행한다. 정상 상태에서는 활성화 명령 세 개를 모두 건너뛰고 설치된 `magisk-insecure-adb` 모듈과 현재 기기 설정에 맡긴다.

## 4. 사용자 화면과 로그

첫 Dialog에서는 `루팅 복구`와 `무시` 중 하나를 선택할 수 있다. `루팅 복구`를 선택한 경우에만 자동 삭제 진행 화면을 표시한다. 모듈 또는 Zygisk 복구 후 재부팅된 기기에서는 기존 LSPosed 후속 설정 Dialog를 표시한다.

앱은 다음 정보를 화면, 진단 화면 및 `RootRecoveryBoot` 로그에 기록한다.

- 검사 시작 시각
- 루트 권한 확인 결과
- ADB 활성화 판정 사유와 실행 여부 및 결과
- ADB 활성화 전후의 `adb_enabled`, `development_settings_enabled`, `init.svc.adbd`, `sys.usb.config`, `sys.usb.state`, `persist.sys.usb.config`, `ro.adb.secure`, `ro.debuggable`
- `resetprop ro.adb.secure 0`, `settings put global adb_enabled 1`, `setprop ctl.start adbd` 각각의 종료 코드와 표준 출력
- 모듈별 디렉터리 존재 여부
- 모듈별 `disable` 파일 존재 여부
- 모듈별 삭제 성공·실패·건너뜀 결과
- 현재 처리 중인 모듈과 전체 처리 진행률
- Zygisk 설정 조회·기록·검증 결과
- Magisk 명령의 원시 출력과 별도로 정규화한 `ZYGISK_STATE=ENABLED|DISABLED|UNKNOWN` 판정
- 재부팅 예약 및 카운트다운 결과
- 접근 거부, timeout, 명령 실패 등 오류 상세

ADB 활성화가 필요한 경우 명령 실행 후 1초 뒤 실제 상태를 다시 읽는다. 단순히 세 명령의 종료 코드가 `0`인 것만으로 성공 처리하지 않고, `ro.adb.secure=0`, `adb_enabled=1`, `init.svc.adbd=running`이 함께 확인되어야 앱 화면에서 ADB 활성화 성공으로 표시한다. 정상 상태에서는 명령을 실행하지 않았음을 표시하고 현재 ADB 상태만 기록한다. 상세 값은 앱 메인 화면의 `현재 부팅 ADB 상세 진단`과 `RootRecoveryBoot` Logcat의 `ADB_DIAG` 항목에 남긴다.

ADB 상세 진단은 생성 즉시 부팅 수신 시각을 식별자로 하여 이력에 저장한다. 새 부팅이 시작되어도 이전 진단을 지우지 않으며 최근 5회 부팅을 최신순으로 보존한다. 같은 부팅에서 진단을 다시 실행하면 해당 부팅 항목만 최신 결과로 교체한다. 앱 메인 화면의 `최근 부팅별 ADB 진단 이력`에서 모듈 활성화 전후 결과를 함께 확인할 수 있어야 한다.

권한 거부 또는 검사 결과 불명확 상태를 `disable 파일 없음`으로 처리하지 않는다. 해당 모듈의 상태를 `UNKNOWN`으로 기록하고 자동 삭제를 수행하지 않는다.

## 5. 안전 조건

- 자동 처리 대상은 코드에 고정된 allowlist로 제한한다.
- 루팅 해제 감지 조건은 `zygisk_lsposed/disable` 파일 존재 또는 Magisk Zygisk 비활성화로 제한한다.
- `/data/adb/modules/*/disable` 같은 전체 모듈 와일드카드 삭제를 사용하지 않는다.
- 자동으로 모듈을 설치하거나 제거하지 않는다.
- 모듈 디렉터리 이름을 사용자 입력으로 받지 않는다.
- 모든 허용 목록 처리가 성공한 경우에만 자동 재부팅한다.
- Zygisk 설정 활성화를 재조회로 확인한 경우에만 자동 재부팅한다.
- 사용자가 `무시`를 선택하면 모듈 파일과 Zygisk 설정을 변경하지 않는다. 복구 상태 감지 단계의 조건부 ADB 활성화 결과는 유지한다.
- 삭제 실패나 상태 확인 불가가 있으면 자동 재부팅하지 않고 오류 상태를 유지한다.
- 일부 모듈만 성공한 경우에도 나머지 모듈 결과를 계속 기록하되 무한 재시도하지 않는다.
- 삭제 전후 결과를 남겨 문제 발생 시 어떤 모듈이 변경됐는지 확인할 수 있어야 한다.

## 6. 비목표

- 허용 목록 밖의 Magisk 모듈 자동 복구
- `zygisk` 이외의 Magisk 설정 데이터 수정
- 모듈 설치, 업데이트, 제거
- 현재 실행 중인 모듈 프로세스의 강제 재시작

## 7. 성공 기준

- 부팅 후 약 10초 뒤 두 복구 조건 검사와 필요한 경우의 ADB 활성화를 하나의 root 셸 세션으로 실행한다.
- `zygisk_lsposed/disable` 파일이 있거나 Zygisk가 꺼져 있을 때 복구 Dialog가 표시된다.
- `루팅 복구`를 누르면 허용 목록 6개 전체를 검사한다.
- `무시`를 누르면 추가 모듈·Zygisk 변경 없이 흐름이 종료된다. 복구 감지에 따른 ADB 활성화 시도는 이미 완료된 상태다.
- `disable` 파일이 있는 허용 모듈의 파일만 삭제된다.
- 허용 목록 밖의 파일과 모듈은 변경되지 않는다.
- 루트 권한 요청은 하나의 root 셸 세션으로 처리된다.
- 파일 삭제 결과와 실패 원인이 진단 화면 및 Logcat에서 확인된다.
- 6개 모듈 처리 직후 Zygisk를 활성화하고 값을 다시 조회하여 확인한다.
- 모듈 디렉터리가 없거나 접근할 수 없는 경우 안전하게 건너뛴다.
- 삭제 중 현재 모듈과 처리 결과가 화면에 표시된다.
- 하나 이상의 파일을 성공적으로 삭제했거나 Zygisk를 새로 활성화하고 최종 검증까지 성공하면 `10초 후 재부팅`이 실행된다.
- 자동 복구 재부팅 다음 부팅에는 LSPosed 후속 설정 Dialog가 표시된다.
- 삭제 대상이 없고 Zygisk도 이미 활성화되어 있거나 오류가 있으면 자동 재부팅하지 않는다.

## 8. 검증 시나리오

1. 허용 목록 모듈 한 개에 `disable` 파일을 만들고 재부팅한다.
2. 약 10초 후 `zygisk_lsposed/disable` 감지로 복구 Dialog가 표시되는지 확인한다.
3. `무시`를 눌렀을 때 ADB는 활성화되어 있고 모듈 파일과 Zygisk 설정은 변경하지 않는지 확인한다.
4. 다시 재부팅한 뒤 `루팅 복구`를 눌러 자동 삭제 화면으로 이동하는지 확인한다.
5. 허용 목록의 여러 모듈에 `disable` 파일을 만들고 모두 처리되는지 확인한다.
6. 허용 목록 밖 모듈에 `disable` 파일을 만들고 파일이 유지되는지 확인한다.
7. 모듈 디렉터리가 없는 상태에서 오류 없이 건너뛰는지 확인한다.
8. 접근 거부 또는 timeout 발생 시 `UNKNOWN`으로 기록되고 삭제하지 않는지 확인한다.
9. 삭제 중 현재 처리 모듈과 결과가 화면에 표시되는지 확인한다.
10. 하나 이상의 파일 삭제가 성공하면 `10초 후 재부팅` 카운트다운이 표시되고 재부팅되는지 확인한다.
11. 재부팅 약 10초 후 LSPosed 설정 Dialog가 표시되는지 확인한다.
12. 복구 완료 후 `zygisk_lsposed/disable`이 없는 정상 부팅에서는 루팅 해제 Dialog가 표시되지 않는지 확인한다.
13. 삭제 실패 또는 확인 불가가 있으면 자동 재부팅하지 않는지 확인한다.
14. Zygisk가 꺼진 상태에서 자동 복구를 실행하면 모듈 처리 직후 Zygisk 설정이 켜지고 검증 결과가 표시되는지 확인한다.
15. `magisk --sqlite`가 없거나 설정 기록에 실패하면 자동 재부팅하지 않는지 확인한다.
16. `disable` 파일 없이 Zygisk만 꺼진 상태에서도 복구 Dialog가 표시되고, 복구 후 자동 재부팅되는지 확인한다.
17. 두 상태가 모두 정상인 부팅에서는 약 10초 뒤 ADB 강제 활성화 명령이 실행되지 않고 `NORMAL`로 기록되는지 확인한다.
18. 상태 확인 실패 시 복구 Dialog는 표시하지 않더라도 ADB 활성화를 시도하고 `STATE_CHECK_FAILED`로 기록하는지 확인한다.
19. `magisk --sqlite` 결과 앞에 경고가 여러 줄 출력되어도 정규화된 Zygisk 상태와 Dialog 판정이 일치하는지 확인한다.
20. 스텁 명령을 사용하는 셸 테스트에서 `NORMAL`은 ADB 명령을 호출하지 않고 `RECOVERY_DETECTED`와 `STATE_CHECK_FAILED`는 세 ADB 명령을 모두 호출하는지 확인한다.
